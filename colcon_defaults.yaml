build:
  mixin:
    - rel-with-deb-info
    - compile-commands
  cmake-args:
    - -DCMAKE_CUDA_ARCHITECTURES=86

  allow-overriding: [
    "mrs_uav_flightforge_simulator",
    "ov_core",
    "ov_eval",
    "ov_init",
    "ov_msckf",
  ]
